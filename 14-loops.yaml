- name: install packages
  hosts: localhost
  become: yes
  tasks:
  - name: print key value pair
    ansible.builtin.package:
      name: "{{item}}"
      state: present
    loop:
    - git
    - mysql
    - postfix